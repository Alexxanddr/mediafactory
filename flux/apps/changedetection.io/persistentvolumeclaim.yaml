apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: changedetection-config
  namespace: extra-tools
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
  volumeMode: Filesystem
  volumeName: changedetection-config
