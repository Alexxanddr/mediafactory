apiVersion: v1
kind: Service
metadata:
  name: changedetection
  namespace: extra-tools
spec:
  selector:
    app: changedetection
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
