apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-wg-data
  namespace: multimedia-factory
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: "100Gi"
  storageClassName: local-path
  volumeMode: Filesystem
  volumeName: qbittorrent-wg-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-wg-config
  namespace: multimedia-factory
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: "100Gi"
  storageClassName: local-path
  volumeMode: Filesystem
  volumeName: qbittorrent-wg-config
---
