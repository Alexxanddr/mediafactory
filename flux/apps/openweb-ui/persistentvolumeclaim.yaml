---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-webui-config
  namespace: extra-tools
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: local-path
  volumeMode: Filesystem
  volumeName: open-webui-config
