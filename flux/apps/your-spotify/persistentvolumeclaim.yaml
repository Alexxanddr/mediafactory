apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spotify-mongo
  namespace: extra-tools
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: local-path
  volumeMode: Filesystem
  volumeName: spotify-mongo