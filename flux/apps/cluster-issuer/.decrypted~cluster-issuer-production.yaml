apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
    name: letsencrypt-production
spec:
    acme:
        email: sottile.alessandro10@gmail.com
        privateKeySecretRef:
            name: letsencrypt-apigateway-production
        server: https://acme-v02.api.letsencrypt.org/directory
        solvers:
            - dns01:
                cloudflare:
                    apiTokenSecretRef:
                        key: api-token
                        name: cert-manager
