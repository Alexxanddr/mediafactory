---
apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: extra-tools
spec:
  type: ClusterIP
  selector:
    app: influxdb
  ports:
  - name: influxdb
    port: 8086
    targetPort: 8086
    protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: scrutiny-web
  namespace: extra-tools
spec:
  type: ClusterIP
  selector:
    app: scrutiny-web
  ports:
  - name: scrutiny-web
    port: 8080
    targetPort: 8080
    protocol: TCP
